CREATE TABLE ISSUESLISTS
(
    ID SERIAL PRIMARY KEY,
    NAME VARCHAR(100) NOT NULL
);

CREATE TABLE ISSUES
(
    ID SERIAL PRIMARY KEY,
    ISSUESLIST_ID BIGINT UNSIGNED NOT NULL,
    SUMMARY VARCHAR(100) NOT NULL,
    DESCRIPTION VARCHAR(1024),
    USER_ID_ASSIGNEDTO BIGINT UNSIGNED,
    FOREIGN KEY (ISSUESLIST_ID) REFERENCES ISSUESLISTS(ID),
    FOREIGN KEY (USER_ID_ASSIGNEDTO) REFERENCES USERS(ID)
);

INSERT INTO ISSUESLISTS (NAME)
VALUES ("ToDo"), ("In progress"), ("Done");

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES
    (1, "Stworzyć bazę danych", "Zaprojektować tabele i relacje.", 1),
    (1, "Przygotować diagram ERD", "Narysować diagram zależności.", 2),
    (1, "Stworzyć repozytorium Git", "Utworzyć repozytorium projektu.", 3),
    (1, "Zaprojektować UI", "Szkic interfejsu użytkownika.", 4),
    (1, "Utworzyć plik README", "Dodać opis projektu.", 5);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES
    (2, "Implementacja logowania", "Dodać obsługę logowania.", 1),
    (2, "Tworzenie API", "Implementacja endpointów REST.", 2),
    (2, "Testy jednostkowe", "Napisać testy dla modułu użytkownika.", 3),
    (2, "Integracja z frontendem", "Połączyć API z interfejsem React.", 4),
    (2, "Konfiguracja Dockera", "Utworzyć pliki Dockerfile.", 5);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES
    (3, "Analiza wymagań", "Zakończono analizę projektu.", 1),
    (3, "Utworzenie repozytorium", "Repozytorium utworzone.", 2),
    (3, "Projekt bazy danych", "Baza zaprojektowana.", 3),
    (3, "Plan projektu", "Harmonogram ustalony.", 4),
    (3, "Spotkanie z zespołem", "Omówiono plan działań.", 5);

COMMIT;

Niestety już scommitowałem a chyba nie powinienem.